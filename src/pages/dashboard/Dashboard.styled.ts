import styled from "@emotion/styled";

export const Pod = styled.section`
    background: #FAFAFA;
    box-shadow: 0px 0px 6px -1px rgba(0, 0, 0, 0.25);
    border-radius: 4px;
    width: 280px;
    height: 190px;
`;
